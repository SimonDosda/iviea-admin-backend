{
  "routes": [
    {
      "method": "GET",
      "path": "/printful/products",
      "handler": "printful.products",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/printful/products/:productId",
      "handler": "printful.product",
      "config": {
        "policies": []
      }
    }
  ]
}
